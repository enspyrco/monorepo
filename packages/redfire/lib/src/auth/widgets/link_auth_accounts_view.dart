import 'package:flutter/material.dart';

class LinkAuthAccountsView extends StatelessWidget {
  const LinkAuthAccountsView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
