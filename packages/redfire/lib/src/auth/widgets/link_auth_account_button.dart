import 'package:flutter/material.dart';

class LinkAuthAccountButton extends StatelessWidget {
  const LinkAuthAccountButton({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
