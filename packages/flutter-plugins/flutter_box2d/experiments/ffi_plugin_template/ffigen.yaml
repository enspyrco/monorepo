# Run with `dart run ffigen --config ffigen.yaml`.
name: FfiPluginTemplateBindings
description: |
  Bindings for `src/ffi_plugin_template.h`.

  Regenerate bindings with `dart run ffigen --config ffigen.yaml`.
output: 'lib/ffi_plugin_template_bindings_generated.dart'
headers:
  entry-points:
    - 'src/ffi_plugin_template.h'
  include-directives:
    - 'src/ffi_plugin_template.h'
preamble: |
  // ignore_for_file: always_specify_types
  // ignore_for_file: camel_case_types
  // ignore_for_file: non_constant_identifier_names
comments:
  style: any
  length: full
