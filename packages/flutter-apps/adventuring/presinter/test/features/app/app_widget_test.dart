import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('X should ... when ...', (tester) async {
    // await tester.pumpWidget();

    // final homeFinder = find.byType(BlahScreen);
  });
}
