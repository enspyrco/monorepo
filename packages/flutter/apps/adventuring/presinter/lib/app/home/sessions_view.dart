import 'package:flutter/material.dart';

class SessionsView extends StatelessWidget {
  const SessionsView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Sessions View'),
    );
  }
}
