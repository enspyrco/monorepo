import 'package:flutter_test/flutter_test.dart';
import 'package:tech_world_challenge_ui/main.dart';

void main() {
  testWidgets('...', (WidgetTester tester) async {
    await tester.pumpWidget(const MyApp());

    expect(true, isTrue);
  });
}
