name: domain_visualiser
description: Draw diagrams to visualise & generate your project domain.
publish_to: 'none'
version: 0.0.1

environment:
  sdk: ">=2.17.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  
  redfire:
    git:
      url: https://github.com/enspyrco/monorepo.git
      path: packages/flutter/utils/redfire
  # cloud_firestore_extensions:
  #   path: ../utils/flutterfire/cloud_firestore_extensions
  firestore_service_interface:
    git:
      url: https://github.com/enspyrco/monorepo.git
      path: packages/dart/interfaces/firestore_service_interface
  firebase_core: ^1.12.0
  firebase_auth: ^3.3.6
  freezed_annotation:
  json_annotation: ^4.4.0
  redux:
  flutter_redux:
  fast_immutable_collections:

  # utils 
  uuid: any
  flutterfire_ui: ^0.4.0
  
dev_dependencies:
  redfire_test:
    git:
      url: https://github.com/enspyrco/monorepo.git
      path: packages/flutter/utils/redfire_test
  redfire_lints:
    git:
      url: https://github.com/enspyrco/monorepo.git
      path: packages/flutter/utils/redfire_lints
  build_runner:
  freezed:
  json_serializable: ^6.0.1

  

flutter:
  uses-material-design: true
