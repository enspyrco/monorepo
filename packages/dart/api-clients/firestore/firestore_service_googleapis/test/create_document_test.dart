import 'package:test/test.dart';

void main() {
  group('End to end API tests for', () {
    setUp(() {
      // Additional setup goes here.
    });

    test('createDocument', () {
      // I made this test as way to prototype the createDocument function
      // but decided to protoype via the example rather than a test
    }, skip: true);
  });
}
