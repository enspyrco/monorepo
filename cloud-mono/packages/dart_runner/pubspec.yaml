name: dart_runner
description: A Dart server that runs Dart code, built with shelf.
version: 0.0.1

environment:
  sdk: '>=2.15.1 <3.0.0'

dependencies:
  shelf: ^1.1.0

dev_dependencies:
  http: ^0.13.0
  lints: ^1.0.0
  test: ^1.15.0
