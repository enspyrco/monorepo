name: redfire
description: A Redux based architecture connected to Firebase streams.
version: 0.0.1
homepage:

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  # firebase 
  firebase_core: "^1.1.0"
  redux: any
  freezed_annotation: any

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
