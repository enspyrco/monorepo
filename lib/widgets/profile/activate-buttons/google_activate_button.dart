import 'package:flutter/material.dart';

class GoogleActivateButton extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MaterialButton(onPressed: () {}, child: Text('ACTIVATE GOOGLE'));
  }
}
